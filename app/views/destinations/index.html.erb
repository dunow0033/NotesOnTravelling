<h1>All Destinations created so far: </h1>

<%= destinations.each do | destination | %>
    <!-- each destination entered, right next that individual destination's page -->
<% end %>

<%= link_to "See Your Destinations", user_path(user) %>
<br>
<%= link_to "Make New Destination", new_destination_path %>